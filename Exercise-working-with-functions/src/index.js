function combine(a, b, c) {
  // Calculate the result based on the formula: a * b / c
  var result = (a * b) / c;
  return result;
}

// Example usage:
var a = 5;
var b = 10;
var c = 2;
var calculationResult = combine(a, b, c);
console.log("The calculated result is: " + calculationResult);
